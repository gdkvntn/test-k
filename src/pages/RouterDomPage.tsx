const RouterDomPage = () => {
  return (
    <div>
      <h2>React Router DOM</h2>
      <p>React Router is a standard library for routing in React.</p>
      <p>
        It enables the navigation among views of various components in
        a React Application, allows changing the browser URL, and
        keeps the UI in sync with the URL.
      </p>
    </div>
  );
};

export default RouterDomPage;
